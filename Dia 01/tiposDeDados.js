// Numero -> number
let idade = 30;

// String -> string
let nome = "João";

// Boolean -> boolean
let estaLogado = true; // ou false

// Lista (Array) -> array
let frutas = ["banana", "maca", "laranja"];

// Objeto -> object
let pessoa = {
    nome: "João",
    idade: 30,
};

console.log(pessoa.nome);